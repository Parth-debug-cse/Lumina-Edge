# Lumina Edge — Runtime Dependencies (Linux / Windows)
# Install with: pip install --break-system-packages -r requirements.txt
#
# NOTE: macOS/Apple Silicon uses scripts/requirements-macos.txt instead.

# Core inference
psutil>=5.9.0
requests>=2.28.0
huggingface-hub>=0.16.0

# RAG (document ingestion and query)
chromadb>=0.4.0
sentence-transformers>=2.2.0
tiktoken>=0.3.0
pymupdf>=1.23.0
python-docx>=0.8.0

# Pipeline API (UC4 - Multi-Agent Pipeline)
fastapi>=0.100.0
uvicorn>=0.23.0
aiohttp>=3.8.0
