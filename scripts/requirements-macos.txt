# macOS Runtime Dependencies (Apple Silicon)
# For running and quantizing models with MLX backend
# - Loads .safetensors directly (no conversion needed)
# - Can quantize existing models to 2/3/4/6/8 bit (recommended: 4 or 8)
# Install with: pip install -r scripts/requirements-macos.txt

# MLX framework for Apple Silicon (required for running and quantization)
mlx>=0.11.0
mlx-lm>=0.9.0

# System utilities
psutil

# RAG dependencies (document ingestion and query)
chromadb>=0.4.0
sentence-transformers>=2.2.0
tiktoken>=0.4.0
PyMuPDF>=1.23.0
python-docx>=1.0.0
requests>=2.28.0

# HuggingFace for model downloads
huggingface-hub>=0.16.0

# Pipeline API (UC4 - Multi-Agent Pipeline)
fastapi>=0.100.0
uvicorn>=0.23.0
aiohttp>=3.8.0

# Lumina Scout — hardware-aware model finder
typer>=0.9
rich>=13.0
httpx>=0.27
psutil>=5.9
dbgpu[fuzz]>=1.0
nvidia-ml-py>=12.0