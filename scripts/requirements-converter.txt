# Model Converter Dependencies (Linux/Windows)
# Converts models to GGUF format for llama.cpp
# Supports: SafeTensors, PyTorch .bin/.pt, HF Sharded models
# Install with: pip install -r scripts/requirements-converter.txt

# Core ML dependencies for conversion
torch>=2.0.0
transformers>=4.30.0
safetensors>=0.3.1
numpy>=1.24.0

# Networking (for HF model downloads)
requests>=2.28.0
huggingface-hub>=0.16.0

# Required: For .gguf file validation and conversion
gguf

